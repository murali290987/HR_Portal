# Copy this file to .env and fill in your real key.
# query.py loads .env via python-dotenv so the key never has to be
# hardcoded in source (and never gets committed if .env is gitignored).
ANTHROPIC_API_KEY=sk-ant-...
